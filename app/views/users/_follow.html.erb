<%= form_for current_user.relationships.build(followed_id: @user.id), remote:
  true do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit t("relationship.follow"), class: "btn btn-large btn-primary" %>
<% end %>
